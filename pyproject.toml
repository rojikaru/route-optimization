[project]
name = "gtfs-tryout"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alex Horyshevskyi", email = "oleksii120@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pip>=26.2.1",
]

[project.scripts]
gtfs-tryout = "gtfs_tryout:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
